# 编程与代码知识

记录如何正确地编写、测试和维护代码，包括：

- Python 语法、标准库、NumPy、pandas 和可视化；
- 函数、类、模块、包、类型标注和异常处理；
- 单元测试、日志、配置、性能和调试；
- 数据管道、API 调用和可复现代码；
- 常用代码片段的原理、适用范围和风险。

正式项目代码放在顶层 `src/`，这里记录代码知识和解释，不把未经验证的片段当作正式模块。
