# 代码要求

本文件是仓库中所有代码必须遵守的最低要求。

## 1. 技术基线

- 使用 Python 3.11 或更高版本。
- 正式代码放在 `src/quant_trading/`。
- 测试放在 `tests/`，测试文件名使用 `test_*.py`。
- 第三方依赖统一写入 `pyproject.toml`，不得只记录在本机环境中。

## 2. 接口与说明

- 公开函数、类和模块必须有 docstring。
- 公开函数必须有类型标注。
- 输入和输出要说明单位、时间频率与数据格式。
- 收益率使用小数表示：`0.01` 表示 1%。
- 年化计算必须明确 `periods_per_year`，不能隐藏频率假设。
- 异常输入必须明确报错，不静默返回误导性结果。

## 3. 量化代码约束

- 禁止在历史决策点使用当时不可获得的未来数据。
- 时间序列训练、验证和测试必须保持时间顺序。
- 交易成本、滑点、成交延迟和复权规则必须显式配置。
- 信号生成、仓位计算、成交模拟和绩效统计应保持独立。
- 计算结果必须可以从固定输入重复得到；使用随机数时固定并暴露种子。
- 最大回撤等指标必须在 docstring 中说明符号方向和计算口径。

## 4. 测试要求

- 每个公开功能至少包含正常输入、边界输入和错误输入测试。
- 修复缺陷时必须先增加能够复现问题的测试。
- 涉及时间序列的测试必须覆盖前视数据风险。
- 提交前运行：

```bash
python3 -m unittest discover -s tests -v
```

## 5. 安全要求

- 禁止提交 API 密钥、密码、Cookie、私钥、账户号和真实认证日志。
- 秘密信息只通过环境变量或专用密钥管理服务读取。
- 日志不得输出凭证、完整订单身份信息或不必要的个人数据。
- 外部数据和交易接口必须设置超时，并对失败进行明确处理。

## 6. 提交要求

- 一次提交只处理一个清晰主题。
- 提交前保证测试通过，并运行 `git diff --check`。
- 不提交缓存、构建产物、编辑器配置或本地生成数据。
