# 正式代码目录

`src/quant_trading/` 存放已经从研究实验中提炼出的可复用 Python 代码。

进入这里的代码应当：接口清楚、输入假设有说明、没有前视数据依赖，并在 `tests/` 中有对应测试。探索性代码应先留在 `notebooks/`，命令行编排放在 `scripts/`。

完整规则见 [`docs/CODE_GUIDE.md`](../docs/CODE_GUIDE.md)。
