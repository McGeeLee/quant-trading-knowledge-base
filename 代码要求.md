# 代码要求

## 文件怎么放

每个任务使用一个独立文件夹，每个老板确认过的版本使用一个独立版本文件夹：

```text
代码/
└── 国信iQuant/
    └── TASK-IQ-001_任务名称/
        ├── v1/
        │   ├── strategy.py
        │   └── 说明.md
        └── v2/
            ├── strategy.py
            └── 说明.md
```

老板提出新要求时，新建 `v2`、`v3`，禁止覆盖已经确认的旧版本。

## 每个版本必须包含

- `strategy.py`：可以放进交易软件运行的完整代码。
- `说明.md`：老板要求、策略逻辑、参数、日期、验收结果和相对上一版的变化。

## 代码可读性

- 文件开头集中定义合约、日期、手数、乘数等参数。
- 函数和变量名称必须表达实际用途。
- 注释重点解释策略原因、撮合时点和风险，避免逐行翻译代码。
- 时间格式、K 线周期、价格单位、手数和合约乘数必须明确。
- 回测结果不能冒充实盘收益。

## 国信 iQuant 要求

- 按 iQuant 环境要求保留 `# coding: gbk`。
- 必须明确区分回测、运行和实盘模式。
- 回测专用代码不得在运行或实盘模式发送订单。
- `ContextInfo`、`buy_open` 等 iQuant 接口的用途要能从代码和说明中看懂。

## 安全要求

- 禁止保存交易密码、API 密钥、Cookie、私钥和账户号。
- 修改为实盘代码前必须重新检查订单方向、手数、重复下单和退出条件。
- 每个版本都要保留可核对的预期输出或验收结果。
